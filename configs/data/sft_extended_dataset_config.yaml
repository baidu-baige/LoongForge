default:
  format: alpaca
  columns:
    prompt: instruction
    query: input
    response: output
    history: history

alpaca:
  format: alpaca
  columns:
    prompt: instruction
    query: input
    response: output
    history: history

