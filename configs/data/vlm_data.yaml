defaults:
  #- ../data@: base
  - _self_ 

#data:
tokenizer_type: 

hf_tokenizer_path: 

data_path:

dataloader_type: external

split: [100, 0, 0]

add_question_in_pretrain: true

enable_discard_sample: true

num_workers: 16