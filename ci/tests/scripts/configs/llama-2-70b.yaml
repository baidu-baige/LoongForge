model_name: llama-2-70b
description: $model_name模型自动化
huggingface_path: https://huggingface.co/meta-llama/Llama-2-70b-hf
bos_path: bos:/cce-ai-datasets/huggingface.co/meta-llama/Llama-2-70b-hf
TOTAL_K8S_NODES: 4

# 模型参数
MODEL_RUNNABLE: True # 表示模型是否支持运行, 或者 True / False

# aiak-training-llm 训练的参数
HF_CKPT_PATH: $pfs_path/huggingface.co/meta-llama/Llama-2-70b-hf
DATA_PATH: $pfs_path/aiak-training-llm/llama2/pile_test/pile-llama_text_document
TOKENIZER_PATH: $pfs_path/huggingface.co/meta-llama/Llama-2-70b-hf
CHECKPOINT_PATH: $step1_output_path
TENSORBOARD_PATH: /workspace/tensorboard/$model_name.log

# 训练参数
TENSOR_MODEL_PARALLER_SIZE: 4
PIPELINE_MODEL_PARALLER_SIZE: 4
log_interval: 1
initial_loss_scale: 1024
global_batch_size: 64

scenarios:
  - function:
      pretrain:
        Step1:
          CONVERT_ARGS: '
            --load_platform=huggingface
            --save_platform=mcore
            --common_config_path=${convert_checkpoint_path}/config/llama2-70b.json
            --tensor_model_parallel_size=${TENSOR_MODEL_PARALLER_SIZE}
            --pipeline_model_parallel_size=${PIPELINE_MODEL_PARALLER_SIZE}
            --load_ckpt_path=${HF_CKPT_PATH}
            --save_ckpt_path=${CHECKPOINT_PATH}
            --no_save_optim
            --no_load_optim'
        Step2:
          MODEL_ARGS: '
            --model-name llama2-70b'
          DATA_ARGS: '
            --tokenizer-type HFTokenizer
            --hf-tokenizer-path $TOKENIZER_PATH
            --data-path $DATA_PATH
            --split 949,50,1'
          TRAINING_ARGS: '
            --training-phase pretrain
            --seq-length 4096
            --max-position-embeddings 4096
            --init-method-std 0.01
            --micro-batch-size 1
            --global-batch-size ${global_batch_size}
            --lr 0.0002
            --min-lr 1.0e-5
            --clip-grad 1.0
            --weight-decay 0.01
            --optimizer adam
            --adam-beta1 0.9
            --adam-beta2 0.95
            --adam-eps 1e-05
            --norm-epsilon 1e-6
            --train-iters ${train_iters}
            --lr-decay-iters 500000
            --lr-decay-style cosine
            --lr-warmup-fraction 0.002
            --initial-loss-scale ${initial_loss_scale}
            --fp16
            --load $CHECKPOINT_PATH
            --save-interval 5000
            --eval-interval 1000
            --eval-iters 0'
            # --save ${step1_output_path}
          MODEL_PARALLEL_ARGS: '
            --tensor-model-parallel-size ${TENSOR_MODEL_PARALLER_SIZE}
            --pipeline-model-parallel-size ${PIPELINE_MODEL_PARALLER_SIZE}
            --use-distributed-optimizer
            --overlap-grad-reduce
            --overlap-param-gather
            --distributed-backend nccl
            --sequence-parallel'
          LOGGING_ARGS: '
            --log-interval ${log_interval}
            --tensorboard-dir $TENSORBOARD_PATH
            --log-timers-to-tensorboard'
          loss_in_value: '[
              1.245013E+00
              1.339952E+00
              1.410553E+00
              1.390803E+00
              1.364848E+00
              1.363091E+00
              1.387946E+00
              1.354392E+00
              1.375608E+00
              1.416134E+00
            ]'
  - perf:
      pretrain:
        Step1:
          TENSOR_MODEL_PARALLER_SIZE: 4
          PIPELINE_MODEL_PARALLER_SIZE: 4
        Step2:
          # 性能场景不需要重新生成ckpt，指定ckpt训练
          CHECKPOINT_PATH: no_need_ckpt_path
          TOTAL_K8S_NODES: 4
          TENSOR_MODEL_PARALLER_SIZE: 4
          PIPELINE_MODEL_PARALLER_SIZE: 4
          global_batch_size: 1024
          initial_loss_scale: 16

tasks:
  check_correctness_task: True
  check_perfness_task: True
