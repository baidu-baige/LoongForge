model_name: baichuan2-7b
description: $model_name模型自动化
huggingface_path: https://huggingface.co/baichuan-inc/Baichuan2-7B-Base
bos_path: bos:/cce-ai-datasets/huggingface.co/baichuan-inc/Baichuan2-7B-Base
TOTAL_K8S_NODES: 1

# 模型参数
MODEL_RUNNABLE: True # 表示模型是否支持运行, 或者 True / False

# aiak-training-llm 训练的参数
HF_CKPT_PATH: $pfs_path/huggingface.co/baichuan-inc/Baichuan2-7B-Base
DATA_PATH: $pfs_path/aiak-training-llm/baichuan2/pile_test/pile-baichuan2_text_document
TOKENIZER_PATH: $pfs_path/huggingface.co/baichuan-inc/Baichuan2-7B-Base
CHECKPOINT_PATH: $step1_output_path
TENSORBOARD_PATH: /workspace/tensorboard/$model_name.log

# 训练参数
TENSOR_MODEL_PARALLER_SIZE: 1
PIPELINE_MODEL_PARALLER_SIZE: 1
log_interval: 1
initial_loss_scale: 1024
global_batch_size: 64

scenarios:
  - function:
      pretrain:
        Step1:
          CONVERT_ARGS: '
            --load_platform=huggingface
            --save_platform=mcore
            --common_config_path=${convert_checkpoint_path}/config/baichuan2-7b.json
            --tensor_model_parallel_size=${TENSOR_MODEL_PARALLER_SIZE}
            --pipeline_model_parallel_size=${PIPELINE_MODEL_PARALLER_SIZE}
            --load_ckpt_path=${HF_CKPT_PATH}
            --save_ckpt_path=${CHECKPOINT_PATH}
            --no_save_optim'
        Step2:
          MODEL_ARGS: '
            --model-name baichuan2-7b'
          DATA_ARGS: '
            --tokenizer-type HFTokenizer
            --hf-tokenizer-path $TOKENIZER_PATH
            --data-path $DATA_PATH
            --split 949,50,1'
          TRAINING_ARGS: '
            --training-phase pretrain
            --seq-length 4096
            --max-position-embeddings 4096
            --init-method-std 0.01
            --micro-batch-size 1
            --global-batch-size ${global_batch_size}
            --lr 0.0002
            --min-lr 1.0e-5
            --clip-grad 1.0
            --weight-decay 0.01
            --optimizer adam
            --adam-beta1 0.9
            --adam-beta2 0.95
            --adam-eps 1e-05
            --norm-epsilon 1e-6
            --train-iters ${train_iters}
            --lr-decay-iters 500000
            --lr-decay-style cosine
            --lr-warmup-fraction 0.002
            --initial-loss-scale ${initial_loss_scale}
            --fp16
            --load $CHECKPOINT_PATH'
            # --save ${step1_output_path}
          MODEL_PARALLEL_ARGS: '
            --tensor-model-parallel-size ${TENSOR_MODEL_PARALLER_SIZE}
            --pipeline-model-parallel-size ${PIPELINE_MODEL_PARALLER_SIZE}
            --use-distributed-optimizer
            --overlap-grad-reduce
            --overlap-param-gather
            --sequence-parallel
            --distributed-backend nccl'
          LOGGING_ARGS: '
            --log-interval ${log_interval}
            --save-interval 5000
            --eval-interval 1000
            --eval-iters 0
            --tensorboard-dir $TENSORBOARD_PATH
            --log-timers-to-tensorboard'
          loss_in_value: '[
              1.607830E+00
              1.691556E+00
              1.703612E+00
              1.638856E+00
              1.628210E+00
              1.601250E+00
              1.681732E+00
              1.571484E+00
              1.614750E+00
              1.598651E+00
            ]'
  - perf:
      pretrain:
        Step1:
          TENSOR_MODEL_PARALLER_SIZE: 1
          PIPELINE_MODEL_PARALLER_SIZE: 1
        Step2:
          # 性能场景不需要重新生成ckpt，指定ckpt训练
          CHECKPOINT_PATH: no_need_ckpt_path
          TOTAL_K8S_NODES: 4
          TENSOR_MODEL_PARALLER_SIZE: 1
          PIPELINE_MODEL_PARALLER_SIZE: 1
          global_batch_size: 1024
          initial_loss_scale: 16

tasks:
  check_correctness_task: True
  check_perfness_task: True