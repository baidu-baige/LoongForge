###################################### 配置参数 ######################################

pfs_path: /mnt/pfs
load_hf_ckpt_path_pre: $pfs_path/leoli
aiak_training_path: /workspace/AIAK-Training-Omni
scripts_root_path: $aiak_training_path/ci/tests/scripts
training_log_path: /workspace/logs

megatron_path: /workspace/AIAK-Megatron
convert_checkpoint_path: $aiak_training_path/tools/convert_checkpoint

save_ckpt_path_pre: $load_hf_ckpt_path_pre/aiak-training-llm/save_ckpt

# 判断训练任务各个阶段任务的状态
model_lock_file_path: $save_ckpt_path_pre/lock_file/$model_name

# Step1: 训练output/ckpt 路径
step1_output_path: $save_ckpt_path_pre/$model_name

# 轮数
train_iters: 10

###################################### 配置参数 ######################################


###################################### 调试阶段 ######################################
# load_hf_ckpt_path_pre: /ssd2/leoli
# aiak_training_path: $load_hf_ckpt_path_pre/baidu/hac-aiacc/AIAK-Training-Omni
# scripts_root_path: $aiak_training_path/ci/tests/scripts
# training_log_path: $scripts_root_path/logs

# 获取日志中lm loss的值
# grep 'lm loss:' training.log | awk -F 'lm loss: ' '{print $2}' | awk '{print $1}'
###################################### 调试阶段 ######################################
